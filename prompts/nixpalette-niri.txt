You are building `nixpalette-niri` — a NixOS/Home Manager flake that integrates
the nixpalette theme framework with the niri scrollable-tiling Wayland compositor.

## What nixpalette provides (the dependency)

nixpalette (github:FTMahringer/nixpalette) is a theme management flake built on
top of Stylix. At build time it writes two files:

  NixOS:        /etc/nixpalette/colors.json
                /etc/nixpalette/themes.json
  Home Manager: $XDG_DATA_HOME/nixpalette/colors.json
                $XDG_DATA_HOME/nixpalette/themes.json

colors.json structure:
  { "themeId": "builtin:base/catppuccin-mocha", "polarity": "dark", "base16": { "base00": "1e1e2e", ... } }

themes.json structure:
  { "<themeId>": { "themeId": "...", "polarity": "...", "base16": { ... }, "meta": { ... } }, ... }

All base16 hex values are plain 6-character strings WITHOUT a leading #.
Standard semantic mapping: base00=background, base05=foreground, base08=red,
base0B=green, base0C=cyan, base0D=blue, base0E=purple.

## What nixpalette-niri should provide

A separate flake with nixosModules.default and homeModules.default that
adds niri-ecosystem theming on top of nixpalette. All options live under the
`nixpalette-niri` namespace.

niri uses a KDL-based configuration file (niri/config.kdl). Home Manager support
for niri is provided by the niri-flake (github:sodiboo/niri-flake) which exposes
programs.niri.settings as a Nix attrset. Prefer the attrset approach over raw KDL
string injection where possible.

### 1. swww — wallpaper (niri does not have a built-in wallpaper setter)
- Declare `nixpalette-niri.swww.enable` (default: true)
- On niri startup (spawn-at-startup in config) run swww-daemon and swww img
- Provide `nixpalette-niri.swww.wallpaper` override option (null or path)
- Support static and animated (GIF) wallpapers — swww handles both

### 2. swaylock — lockscreen (works on niri via swaylock-effects or just swaylock)
- Declare `nixpalette-niri.swaylock.enable` (default: true)
- Generate swaylock config from base16 colors (same mapping as nixpalette-sway):
    inside-color  → base00
    ring-color    → base0D
    ring-ver-color → base0B
    ring-wrong-color → base08
    text-color    → base05
    line-color    → base03

### 3. waybar
- Declare `nixpalette-niri.waybar.enable` (default: false)
- Inject CSS @define-color variables for all 16 base16 slots
- niri-specific: waybar needs `layer-shell` output; document the required
  waybar config for niri (use `sway/workspaces` module with niri IPC)

### 4. mako — notification daemon
- Declare `nixpalette-niri.mako.enable` (default: true)
- Same color mapping as nixpalette-sway

### 5. niri border and focus colors
- Declare `nixpalette-niri.borders.enable` (default: true)
- Set via programs.niri.settings.layout.border:
    active-color   → base0D (blue)
    inactive-color → base03 (comments/dim)
- Set programs.niri.settings.layout.focus-ring:
    active-color   → base0E (purple — focus ring is distinct from border in niri)
    inactive-color → base02

### 6. fuzzel — niri's natural launcher companion
- Declare `nixpalette-niri.fuzzel.enable` (default: true)
- Generate programs.fuzzel.settings colors from base16:
    background → base00 + "ff" (add alpha)
    text       → base05 + "ff"
    match      → base0D + "ff"
    selection  → base02 + "ff"
    selection-text → base05 + "ff"
    border     → base0D + "ff"

### 7. Live theme switcher script
- Provide package `nixpalette-niri.packages.${system}.nixpalette-switch`
- Script reads themes.json, presents fzf or fuzzel picker, then on selection:
    1. swww img <wallpaper>
    2. Writes ~/.cache/nixpalette/active.json
    3. Reloads waybar (pkill -SIGUSR2 waybar)
    4. makoctl reload
    5. notify-send confirmation
- Works only for themes in themes.json (nixpalette.preloadThemes)

## Architecture requirements

- flake inputs:
    inputs.nixpalette.url = "github:FTMahringer/nixpalette"
    inputs.niri-flake.url = "github:sodiboo/niri-flake"  # for HM programs.niri
- Re-export nixpalette's modules; users import only nixpalette-niri
- Do NOT re-declare nixpalette options
- All features default to true for core (swww, swaylock, mako, borders, fuzzel),
  false for potentially conflicting (waybar)

## Files to create

- flake.nix
- modules/nixos.nix
- modules/hm.nix
- modules/swww.nix
- modules/swaylock.nix
- modules/waybar.nix
- modules/mako.nix
- modules/borders.nix
- modules/fuzzel.nix
- pkgs/nixpalette-switch/
- README.md

## README should document

- Prerequisites: niri-flake in system flake inputs, nixpalette.preloadThemes set
- Import instructions for all three flakes (nixpalette, niri-flake, nixpalette-niri)
- nixpalette-switch usage
- Each module's options table
- Note on niri's KDL config vs Nix attrset approach
